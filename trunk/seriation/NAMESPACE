import(TSP)

export(
    bertinplot,
    c.seriation,
    criterion, 
    dissplot,
    get_permutation,
    hmap,
    panel.bars, 
    panel.circles, 
    panel.squares, 
    panel.lines,
    permutation,
    pimage, 
    rearrange,
    seriate,
    seriation
    )

S3method(criterion, matrix)
S3method(criterion, dist)

S3method(get_permutation, integer)
S3method(get_permutation, hclust)

S3method(length, ser_permutation)

S3method(pimage, matrix)
S3method(pimage, dist)

S3method(plot,cluster_dissimilarity_matrix)

S3method(print, ser_permutation)
S3method(print, ser_seriation)
S3method(print,cluster_dissimilarity_matrix)

S3method(rearrange, array)
S3method(rearrange, matrix)
S3method(rearrange, numeric)
S3method(rearrange, list)
S3method(rearrange, dist)

S3method(seriate, hclust)
S3method(seriate, matrix)
S3method(seriate, dist)

useDynLib(seriation)
