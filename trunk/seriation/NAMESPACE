importFrom(stats, reorder)  # reorder exists for factor
import(TSP)

export(
    rearrange,
    criterion, 
    bertinplot,
    panel.bars, panel.circles, panel.squares, panel.lines,
    pimage, 
    dissplot,
    hmap,
    Order,
    reorder.hclust      # this is just so we win over gclus
    )


S3method(rearrange, matrix)
S3method(rearrange, dist)

S3method(criterion, matrix)
S3method(criterion, dist)

S3method(reorder, hclust)
S3method(reorder, matrix)
S3method(reorder, dist)

S3method(pimage, matrix)
S3method(pimage, dist)

S3method(print,Order)

S3method(print,cluster_dissimilarity_matrix)
S3method(plot,cluster_dissimilarity_matrix)


useDynLib(seriation)
